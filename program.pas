program
var
integer a := 4, b := 2, c := 6, d := 8;
character f, moo := 'd';
boolean lol := true, defFalse := 0;
begin

while a < b and c = d or b <> c and NOT a * b < c do 
BEgiN
    d := c * a;
    a := b;
    repEaT
    	a := b + 1;
    Until a > d
ENd;

d := a;

end.
